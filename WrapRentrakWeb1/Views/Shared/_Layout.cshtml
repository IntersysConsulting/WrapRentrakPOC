<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>@ViewBag.Title</title>

</head>
<body>
    @*<nav id="myNavbar" class="navbar navbar-default navbar-inverse navbar-fixed-top" role="navigation">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbarCollapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Reports", "Index", "Home", new { }, new { @class = "navbar-brand" })
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="nav navbar-nav">
                <li class="active">
                    @Html.ActionLink("Single Market", "Index", "Home", new { category = "Single Day" }, null)
                </li>
                <li class="active">
                    @Html.ActionLink("Multi Market", "Index", "Home", new { category = "Multi Day" }, null)
                </li>
                <li class="active">
                    @Html.ActionLink("Station Grid", "Index", "Home", new { category = "Station Grid" }, null)
                </li>
            </ul>
        </div>
    </nav><br /><br /><br /><br />*@
    @RenderBody()
   
</body>
</html>